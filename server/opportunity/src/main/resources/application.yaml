server:
  port: 8081

spring:
  application:
    name: opportunity

  datasource:
    url: jdbc:postgresql://64.188.97.232:5432/tramplin
    username: tramplin_adm
    password: tramplin_adm

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

auth:
  service:
    url: ${AUTH_SERVICE_URL:http://auth:9999}

internal-api:
  api-key: moderation-api-key
