server:
  port: ${PORT:8080}

spring:
  application:
    name: profile
  datasource:
    url: ${PROFILE_DB_URL}
    username: ${PROFILE_DB_USERNAME}
    password: ${PROFILE_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: ProfileHikariPool
      maximum-pool-size: 8
      minimum-idle: 2
      idle-timeout: 300000
      max-lifetime: 540000
      keepalive-time: 240000
      connection-timeout: 30000
      validation-timeout: 5000

  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 60MB

auth:
  service:
    url: ${AUTH_SERVER_HOST:http://localhost:9999}

media:
  service:
    url: ${MEDIA_SERVER_URL:http://localhost:8091}

moderation:
  service:
    url: ${MODERATION_SERVER_URL:http://localhost:8082}

opportunity:
  service:
    url: ${OPPORTUNITY_SERVER_URL:http://localhost:8081}

interaction:
  service:
    url: ${INTERACTION_SERVER_URL:http://localhost:8083}

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

internal-api:
  api-key: ${INTERNAL_API_KEY}

springdoc:
  paths-to-match: /api/**
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true
    try-it-out-enabled: true
    filter: true
